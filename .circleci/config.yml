version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:14.17 # Choose an appropriate Node.js version
    working_directory: ~/repo

    steps:
      - checkout

      # Install dependencies
      - restore_cache:
          keys:
            - v1-dependencies-{{ checksum "package-lock.json" }}
            # Add more cache keys if needed

      - run:
          name: Install Dependencies
          command: npm i

      - run:
          name: Install Dependencies
          command: apt-get install libgtk2.0-0 libgtk-3-0 libgbm-dev libnotify-dev libgconf-2-4 libnss3 libxss1 libasound2 libxtst6 xauth xvfb

      - save_cache:
          paths:
            - node_modules
          key: v1-dependencies-{{ checksum "package-lock.json" }}
            # Add more cache keys if needed

      # Run tests using Cypress
      - run:
          name: Run Cypress Tests
          command: npx cypress run 
      - run:
          command: |
            npm install --save-dev cypress-multi-reporters mocha-junit-reporter
            cypress run --reporter cypress-multi-reporters --reporter-options configFile=reporter-config.json 
      - store_test_results:
          path: cypress/test-results/mocha
