version: 2.1
orbs:
  cypress: cypress-io/cypress@3
workflows:
  test:
    jobs:
      - cypress/run:
          cypress-command: 'npx cypress run'
          start-command: 'npx mochawesome-merge cypress/report/mochawesome-report/*.json > cypress/report/combine-report/$(date +"%m-%d-%Y")_final-result.json'
          
          

  

    
      
          


          

